# IDME
Music guessing game!

## GitHub Pages Deploy

This project is configured for GitHub Pages under the `/IDME/` repository path.

- Build output is written to `dist`
- GitHub Pages deployment is handled by `.github/workflows/deploy.yml`
- If you rename the repository, update `base` in `vite.config.ts` to match the new repo name
